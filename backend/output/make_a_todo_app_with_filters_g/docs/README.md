# **Make a Todo App with Filters G**
## Description
This project is a todo list application that allows users to add tasks, mark them as complete, and filter tasks by their status. The application consists of a backend server that handles task operations and a frontend interface that provides a user-friendly experience. The goal of this project is to test the synchronization between the UI and backend, conditional rendering, and data consistency.

## Tech Stack
* Express.js for the backend server
* HTML, CSS, and JavaScript for the frontend
* Node.js as the runtime environment

## Project Structure
```
make_a_todo_app_with_filters_g
├── server.js
├── frontend
│   ├── index.html
│   ├── style.css
│   ├── app.js
```

## Getting Started
### Prerequisites
* Node.js version 16 or higher
* npm (Node Package Manager) installed
### Installation steps
1. Clone the repository to your local machine
2. Run `npm install` in the project root directory to install dependencies
### Running the Backend
1. Navigate to the project root directory
2. Run `node server.js` to start the backend server
### Running the Frontend
1. Navigate to the `frontend` directory
2. Open `index.html` in a web browser to access the todo list application

## API Endpoints
| Method | Path | Description |
| --- | --- | --- |
| POST | /tasks | Add a new task |
| PATCH | /tasks/:id | Mark a task as complete |
| GET | /tasks | Retrieve all tasks |
| GET | /tasks?status=completed | Retrieve completed tasks |
| GET | /tasks?status=pending | Retrieve pending tasks |

## Features
* Add new tasks
* Mark tasks as complete
* Filter tasks by status (completed/pending)
* User-friendly interface with checkbox UI and filter buttons

## Generated by SYNAPSE